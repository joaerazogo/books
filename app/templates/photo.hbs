<h1>Photos:</h1>
<table class="table table-bordered" border="9">
  <thead>
    <tr>
      <th>Title</th>
      <th>imageableId</th>
      <th>imageableType</th>
      <th>photo</th>
    </tr>
  </thead>
  {{#each model as |photo|}}
    <tbody>
      <tr>
        <td>
          {{photo.imageableId}}
        </td>
        <td>
          {{photo.imageableType}}
        </td>
        <td>
          {{photo.title}}
        </td>
        <td>
          <img src="{{photo.uri}}" alt="">
        </td>
      </tr>
    </tbody>
  {{/each}}
</table>
{{outlet}}
